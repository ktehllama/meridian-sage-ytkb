# Channel queue reference file — NOT read by pipeline.py directly
# Copy these commands and run them to populate the queue.
#
# Usage:
#   python pipeline.py queue add @channel [--limit N] [--sort-by date|views] [--whitelist file.txt]
#   python pipeline.py queue run       <- start processing (auto-stops on IP ban)
#   python pipeline.py queue status    <- check what's pending/done/paused
#   python pipeline.py queue reset     <- after IP change: reset paused -> pending, then run again
#
# -------------------------------------------------------
# QUEUE COMMANDS — edit and paste these into your terminal
# -------------------------------------------------------

# High-priority channels (sort by most viewed):
# python pipeline.py queue add @ycombinator --limit 200 --sort-by views
# python pipeline.py queue add @paulgraham --limit 50 --sort-by views

# Standard channels (newest first):
# python pipeline.py queue add @levelsio --limit 100
# python pipeline.py queue add @someotherchannel --limit 50

# Specific videos only (whitelist):
# python pipeline.py queue add @mixedchannel --whitelist my_picks.txt

# After adding all channels, kick off the run:
# python pipeline.py queue run
